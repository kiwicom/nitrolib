# Translate

Translates the supplied key.

> The text output can be transformed using the `transform` function.

Context needs:
* **intl**

**Example:**
```js
const Submit = () => (
  <Button>
    <Translate t="Submit" />
  </Button>
);
```
