// @flow strict

import * as React from "react";

type Props = {|
  label: string,
  value: ?Date,
  error?: string,
  onChange: (?Date) => void,
|};

declare export default React.ComponentType<Props>;
