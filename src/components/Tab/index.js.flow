// @flow strict
import * as React from "react";

type Props = {|
  id: string,
  onClick: string => void,
  active: boolean,
  children: React.Node,
|};

declare export default React.ComponentType<Props>;
