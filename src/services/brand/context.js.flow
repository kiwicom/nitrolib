// @flow strict
import * as React from "react";

import { Brand } from "../../records/Brand";

declare export var context: React.Context<Brand>;

export const { Consumer, Provider } = context;

export default context;
