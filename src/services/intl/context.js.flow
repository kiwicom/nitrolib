// @flow strict
import * as React from "react";

import { intlDefault } from "../../records/Intl";

const ctx = React.createContext(intlDefault);

declare export var Consumer: typeof ctx.Consumer;
declare export var Provider: typeof ctx.Provider;
