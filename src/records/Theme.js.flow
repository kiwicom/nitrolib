// @flow strict
import type { Tokens } from "@kiwicom/orbit-design-tokens";

import type { Brand } from "./Brand";

export type Theme = {|
  orbit: Tokens,
|};

export type ThemeProps = {| theme: Theme |};

declare export var themeDefault: Theme;

declare export function getBrandTheme(brand: Brand): Theme;
