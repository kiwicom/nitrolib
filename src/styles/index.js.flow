// @flow strict
declare export var border: {|
  +size: number,
  +radius: number,
|};

declare export var padding: {|
  +base: number,
  +page: number,
|};

declare export var navbar: {|
  +size: number,
  +height: number,
|};

declare export var logo: {|
  +width: number,
  +height: number,
|};

declare export var language: {|
  // used for decide, when to render 2/3 columns
  // and when to activate customHeight on Popup
  +limit: number,
  +row: {|
    +height: number,
    +width: number,
  |},
|};
