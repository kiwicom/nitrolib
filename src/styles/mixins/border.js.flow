// @flow strict
import type { Interpolation } from "styled-components";

type Props = {
  error: boolean,
  hint: boolean,
  active: boolean,
  visited: boolean,
};

type State = "error" | "success" | "base" | "hint";

declare export function getBorderState(props: Props): State;

declare export default Interpolation;
